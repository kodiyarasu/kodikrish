#include <stdio.h>
void main() 
{
int n,sum=0,i,avg;
int a[i];
printf("Enter the value:");
scanf("%d",&n);
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);	
}
for(i=0;i<n;i++)
{
sum=sum+a[i];
}
avg=sum/n;
printf("%d",avg);
}
