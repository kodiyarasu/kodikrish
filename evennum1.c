#include<stdio.h>
#include<conio.h>
void main()
{
int a,b,i;
printf("Enter the first region:");
scanf("%d",&a);
printf("Enter the final region:");
scanf("%d",&b);
for(i=a;i<=b;++);
{
if(a%2==0)
{
printf("\n Even number",a;
}
}
